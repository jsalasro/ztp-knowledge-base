---
apiVersion: cluster.open-cluster-management.io/v1beta1 
kind: Placement
metadata:
  name: mano-placement-standby
  namespace: policies-mano
spec:
  clusterSets:
    - mano-clusters
  predicates:
    - requiredClusterSelector:
        claimSelector:
          matchExpressions:
            - key: custom.hub-status
              operator: In
              values:
               - standby
        labelSelector:
          matchLabels:
            gitops_acm_config: Active
...